import { ColorPuntajePipe } from './color-puntaje.pipe';

describe('ColorPuntajePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorPuntajePipe();
    expect(pipe).toBeTruthy();
  });
});
